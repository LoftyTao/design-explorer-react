name: Build Windows Executable

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # ----------------------------------------------------------------
      # 终极修复 1：全域路径修正 (Pre-build Patch)
      # [升级点] 1. 同时扫描 src 和 public 目录 (防止漏掉 json 配置文件)
      # [升级点] 2. 使用正则表达式替换，无视引号差异
      # ----------------------------------------------------------------
      - name: Patch Source & Public Code for Electron
        run: |
          $script = @'
          const fs = require('fs');
          const path = require('path');

          // 定义需要替换的文件夹关键词
          const keywords = [
            'Multiple -Image-Layouts',
            'daylight-factor',
            'box-without-img'
          ];

          // 构造正则：匹配以 / 开头，后面跟着关键词的路径
          // 捕获组 $1: 引号 (" or ' or `)
          // 捕获组 $2: 文件夹名
          const regex = new RegExp(`(["'\`])\/(${keywords.join('|')})`, 'g');

          // 递归遍历函数
          function walk(dir) {
            let results = [];
            if (!fs.existsSync(dir)) return results;
            const list = fs.readdirSync(dir);
            list.forEach(function(file) {
              file = path.join(dir, file);
              const stat = fs.statSync(file);
              if (stat && stat.isDirectory()) {
                results = results.concat(walk(file));
              } else {
                // 扫描 js, ts, jsx, tsx 和 json 文件
                if (file.match(/\.(js|jsx|ts|tsx|json)$/)) {
                  results.push(file);
                }
              }
            });
            return results;
          }

          // [关键] 同时扫描 src 和 public 文件夹
          const srcFiles = walk('src');
          const publicFiles = walk('public');
          const allFiles = [...srcFiles, ...publicFiles];

          let patchCount = 0;

          allFiles.forEach(file => {
            let content = fs.readFileSync(file, 'utf8');
            let original = content;
            
            // 执行正则替换：将 "/FolderName" 替换为 "./FolderName"
            // $1 保留原来的引号，./$2 添加相对路径前缀
            content = content.replace(regex, '$1./$2');

            if (content !== original) {
              fs.writeFileSync(file, content);
              console.log(`[Patched] ${file}`);
              patchCount++;
            }
          });
          
          console.log(`Total files patched: ${patchCount}`);
          '@
          
          $script | Out-File -FilePath patch-source.cjs -Encoding utf8
          node patch-source.cjs

      # ----------------------------------------------------------------
      # 修复步骤 2：配置 Vite 相对路径
      # ----------------------------------------------------------------
      - name: Configure Vite Base Path
        run: |
           node -e "
           const fs = require('fs');
           const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
           pkg.homepage = '.';
           
           if (pkg.scripts && pkg.scripts.build) {
             if (pkg.scripts.build.includes('vite build')) {
                if (!pkg.scripts.build.includes('--base')) {
                   pkg.scripts.build = pkg.scripts.build.replace('vite build', 'vite build --base=./');
                }
             } else if (pkg.scripts.build.trim() === 'vite') {
               pkg.scripts.build = 'vite build --base=./';
             }
           }
           
           fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));
           console.log('Updated build script to:', pkg.scripts.build);
           "

      - name: Build for production
        run: npm run build

      # ----------------------------------------------------------------
      # 调试步骤：列出 dist 目录内容
      # 这一步会打印出打包后的文件结构，证明 public 里的文件确实在里面
      # ----------------------------------------------------------------
      - name: List dist directory (Debug)
        run: |
          Get-ChildItem -Recurse dist | Select-Object FullName

      - name: Install Electron and Electron Builder
        run: |
          npm install --save-dev electron electron-builder

      # ----------------------------------------------------------------
      # 修复步骤 3：生成 Electron 主进程
      # ----------------------------------------------------------------
      - name: Create Electron main process file
        run: |
          $content = @"
          const { app, BrowserWindow } = require('electron');
          const path = require('path');

          function createWindow() {
            const win = new BrowserWindow({
              width: 1400,
              height: 900,
              webPreferences: {
                nodeIntegration: false,
                contextIsolation: true,
                webSecurity: false
              }
            });
            
            // 使用 path.join 确保路径解析正确
            win.loadFile(path.join(__dirname, 'dist', 'index.html'));

            // 保持 DevTools 开启
            win.webContents.openDevTools();
          }

          app.whenReady().then(createWindow);

          app.on('window-all-closed', () => {
            if (process.platform !== 'darwin') app.quit();
          });

          app.on('activate', () => {
            if (BrowserWindow.getAllWindows().length === 0) createWindow();
          });
          "@
          $content | Out-File -FilePath electron-main.cjs -Encoding utf8

      # ----------------------------------------------------------------
      # 修复步骤 4：更新 package.json
      # ----------------------------------------------------------------
      - name: Update package.json for Electron
        run: |
          node -e "const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); pkg.main = 'electron-main.cjs'; pkg.build = {appId: 'com.designexplorer.app', productName: 'Design Explorer', files: ['dist/**/*', 'electron-main.cjs', 'package.json'], win: {target: 'nsis', icon: 'public/icon.ico'}}; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));"

      - name: Build Windows executable
        run: npx electron-builder --win --x64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get version from package.json
        id: get_version
        run: |
          node -e "console.log('VERSION=' + require('./package.json').version)" >> $env:GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: design-explorer-windows
          path: dist/*.exe
          retention-days: 30

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: dist/*.exe
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
