name: Build Windows Executable

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # ----------------------------------------------------------------
      # 1. 同步版本号 (Git Tag -> package.json)
      # ----------------------------------------------------------------
      - name: Sync Version from Tag
        if: startsWith(github.ref, 'refs/tags/')
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: |
          node -e "
          const fs = require('fs');
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const version = process.env.TAG_NAME.replace(/^v/, '');
          pkg.version = version;
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));
          console.log('Updated package.json version to:', version);
          "

      # ----------------------------------------------------------------
      # 2. 激进路径修正 (修复资源加载 404 问题)
      # ----------------------------------------------------------------
      - name: Aggressive Path Patching
        run: |
          $script = @'
          const fs = require('fs');
          const path = require('path');

          const targets = [
            '/Multiple -Image-Layouts',
            '/daylight-factor',
            '/box-without-img'
          ];

          function walk(dir) {
            let results = [];
            if (!fs.existsSync(dir)) return results;
            const list = fs.readdirSync(dir);
            list.forEach(function(file) {
              file = path.join(dir, file);
              const stat = fs.statSync(file);
              if (stat && stat.isDirectory()) {
                results = results.concat(walk(file));
              } else {
                if (file.match(/\.(js|jsx|ts|tsx|json|css|html)$/)) {
                  results.push(file);
                }
              }
            });
            return results;
          }

          const allFiles = [...walk('src'), ...walk('public')];
          let patchCount = 0;

          allFiles.forEach(file => {
            let content = fs.readFileSync(file, 'utf8');
            let original = content;
            
            targets.forEach(tgt => {
              const replacement = '.' + tgt;
              if (content.includes(tgt)) {
                 content = content.split(tgt).join(replacement);
              }
            });

            if (content !== original) {
              fs.writeFileSync(file, content);
              console.log(`[Patched] ${file}`);
              patchCount++;
            }
          });
          console.log(`Total files patched: ${patchCount}`);
          '@
          
          $script | Out-File -FilePath patch-source.cjs -Encoding utf8
          node patch-source.cjs

      # ----------------------------------------------------------------
      # 3. 配置 Vite 相对路径
      # ----------------------------------------------------------------
      - name: Configure Vite Base Path
        run: |
           node -e "
           const fs = require('fs');
           const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
           pkg.homepage = '.';
           
           if (pkg.scripts && pkg.scripts.build) {
             if (pkg.scripts.build.includes('vite build')) {
                if (!pkg.scripts.build.includes('--base')) {
                   pkg.scripts.build = pkg.scripts.build.replace('vite build', 'vite build --base=./');
                }
             } else if (pkg.scripts.build.trim() === 'vite') {
               pkg.scripts.build = 'vite build --base=./';
             }
           }
           fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));
           "

      - name: Build for production
        run: npm run build

      - name: Install Electron and Electron Builder
        run: |
          npm install --save-dev electron electron-builder

      # ----------------------------------------------------------------
      # 4. 生成 Electron 主进程
      # ----------------------------------------------------------------
      - name: Create Electron main process file
        run: |
          $content = @"
          const { app, BrowserWindow } = require('electron');
          const path = require('path');

          function createWindow() {
            const win = new BrowserWindow({
              width: 1400,
              height: 900,
              webPreferences: {
                nodeIntegration: false,
                contextIsolation: true,
                webSecurity: false
              }
            });
            
            win.loadFile(path.join(__dirname, 'dist', 'index.html'));
            win.webContents.openDevTools();
          }

          app.whenReady().then(createWindow);

          app.on('window-all-closed', () => {
            if (process.platform !== 'darwin') app.quit();
          });

          app.on('activate', () => {
            if (BrowserWindow.getAllWindows().length === 0) createWindow();
          });
          "@
          $content | Out-File -FilePath electron-main.cjs -Encoding utf8

      # ----------------------------------------------------------------
      # 5. [关键修复] 配置安装包 (NSIS & ArtifactName)
      # 改用生成文件的方式执行，彻底解决 PowerShell 转义报错
      # ----------------------------------------------------------------
      - name: Update package.json for Electron Config
        run: |
          $script = @'
          const fs = require('fs'); 
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8')); 
          
          pkg.main = 'electron-main.cjs'; 
          
          pkg.build = {
            appId: 'com.designexplorer.app', 
            productName: 'Design Explorer',
            files: ['dist/**/*', 'electron-main.cjs', 'package.json'],
            // 下面这行字符串中的 ${} 将被原样保留，供 Electron Builder 使用
            artifactName: '${productName}-Setup-${version}.${ext}',
            win: {
              target: 'nsis', 
              icon: 'public/icon.ico'
            },
            nsis: {
              oneClick: false,
              allowToChangeInstallationDirectory: true,
              createDesktopShortcut: true,
              perMachine: false
            }
          }; 
          
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));
          '@

          $script | Out-File -FilePath update-config.cjs -Encoding utf8
          node update-config.cjs

      - name: Build Windows executable
        run: npx electron-builder --win --x64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: design-explorer-windows
          path: dist/*.exe
          retention-days: 30

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: dist/*.exe
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
